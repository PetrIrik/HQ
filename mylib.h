#pragma once

namespace mytask1 {
	float* IN_array(const size_t size);
	void Prinå_Array(const float* array, const size_t size, size_t otricat, size_t positive);
	size_t input(size_t size);
}

namespace mytask2 {
	size_t input1(size_t a);
}

